# dependencies
node_modules/

# build output
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# secrets — .env.example is committed, .env is not
.env
.env.local
.env*.local

# tooling
.claude/
.vercel/
*.log

# Every write-up is held back until reviewed — all still present locally.
# The corpus is the exception: seeding reads it, so it has to ship.
*.md
!docs/id/*.md

# The take-home note is delivered with the submission, not through the
# repository. Its exported copies must not slip back in either.
*.pdf
*.docx
docs/*.drawio

# OS
.DS_Store
Thumbs.db
!README.md
node_modules
